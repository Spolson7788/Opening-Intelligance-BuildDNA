The service provider gives us a cross-reference from a manufacturer's product to their own ordering number. I import it here as a file: manufacturer, product model, provider S K U. Two mappings saved. The card states the limit itself: a mapping never establishes installed-product identity.
